# Generated by scripts/setup_android_signing.sh — do not commit key.properties.
#
# storeFile is resolved from android/app/ unless an absolute path is used.

storePassword=your-store-password
keyPassword=your-key-password
keyAlias=upload
storeFile=upload-keystore.jks
