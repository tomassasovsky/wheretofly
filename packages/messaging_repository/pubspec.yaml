name: messaging_repository
description: Repository for messaging and notification preferences.
version: 1.0.0
publish_to: "none"

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  messaging_api_client:
    path: ../messaging_api_client

dev_dependencies:
  mocktail: ^1.0.4
  test: ^1.25.0
  very_good_analysis: ^6.0.0
