name: backend_zones_api_client
description: Authenticated backend zone feed client with ETag caching (Flutter).
version: 1.0.0
publish_to: "none"

environment:
  sdk: ">=3.6.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  storage:
    path: ../storage
  zones_api_client:
    path: ../zones_api_client

dev_dependencies:
  mocktail: ^1.0.4
  test: ^1.25.0
  very_good_analysis: ^6.0.0
