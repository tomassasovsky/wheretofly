name: geocoding_repository
description: Repository for geocoding queries with offline caching.
version: 1.0.0
publish_to: "none"

environment:
  sdk: ">=3.6.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  geocoding_api_client:
    path: ../geocoding_api_client
  latlong2: ^0.9.1
  storage:
    path: ../storage

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.0
  test: ^1.25.0
  very_good_analysis: ^6.0.0
