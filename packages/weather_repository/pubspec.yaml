name: weather_repository
description: Repository for weather data at map points.
version: 1.0.0
publish_to: "none"

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  latlong2: ^0.9.1
  weather_api_client:
    path: ../weather_api_client

dev_dependencies:
  mocktail: ^1.0.4
  test: ^1.25.0
  very_good_analysis: ^6.0.0
