name: argentina_bounds
description: Argentina map coverage checks (excludes neighboring countries in the shared bbox).
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  latlong2: ^0.9.1

dev_dependencies:
  test: ^1.25.0
  very_good_analysis: ^7.0.0
