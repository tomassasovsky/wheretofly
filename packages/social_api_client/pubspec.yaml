name: social_api_client
description: HTTP client for Dónde Volar social API.
version: 1.0.0
publish_to: "none"

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  http: ^1.2.0

dev_dependencies:
  test: ^1.25.0
  very_good_analysis: ^6.0.0
